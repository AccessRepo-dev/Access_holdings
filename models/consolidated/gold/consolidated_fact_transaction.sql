{{ config(
    materialized = 'incremental',
    incremental_strategy = 'merge',
    unique_key = ['TRANSACTIONS_UNIQUE_ID','SOURCESYSTEM','COMPANY']
) }}

select
    TRANSACTIONS_UNIQUE_ID,
    TRANSACTION,
    TRANID,
    TRANSACTIONLINE,
    TO_VARCHAR(ACCOUNT) AS ACCOUNT,
    ITEM,
    CLASS,
    CHART_OF_ACCOUNT_UNIQUE_ID,
    POSTINGPERIOD AS POSTINGPERIOD,
    EMPLOYEE,
    ENTITY,
    BILLINGADDRESS,
    SHIPPINGADDRESS,
    CURRENCY,
    CONSOLIDATED_EXCHANGE_RATE_UNIQUE_ID,
    SUBSIDIARY,
    NULL AS LOCATION,
    NULL AS LOCATIONNAME,
    CREATEDFROM,
    STATUS,
    DEPARTMENT,
    ACCOUNTINGBOOK,
    POSTING_PERIOD_DATE,
    ACCTNUMBER,
    ACCTTYPE,
    ACCOUNT_NAME,
    TYPE,
    TRANSACTIONNUMBER,
    NETAMOUNT,
    AMOUNT,
    CONVERTED_NET_AMOUNT,
    TRANSACTIONLINETYPE,
    ITEMTYPE,
    ACCOUNTINGLINETYPE,
    TITLE,
    QUANTITY,
    BOM_QUANTITY,
    MEMO,
    BILLINGSTATUS,
    NULL AS TRANSACTION_SESSION_RO,
    POSTING,
    TRANDATE,
    STARTDATE,
    ENDDATE,
    DUEDATE,
    CLOSEDATE,
    LASTMODIFIEDDATE,
    'NETSUITE' AS SOURCESYSTEM,
    'WAGWAY' AS COMPANY,
    CURRENT_TIMESTAMP()::TIMESTAMP_NTZ AS CONSOLIDATED_GOLD_LOAD_DATE
from {{ env_var('DBT_WAGWAY','wagway_dev') }}.gold.NETSUITE_FACT_TRANSACTION

union all

select
    TRANSACTIONS_UNIQUE_ID,
    TRANSACTION,
    TRANID,
    TRANSACTIONLINE,
    TO_VARCHAR(ACCOUNT) AS ACCOUNT,
    ITEM,
    CLASS,
    CHART_OF_ACCOUNT_UNIQUE_ID,
    POSTINGPERIOD AS POSTINGPERIOD,
    EMPLOYEE,
    ENTITY,
    BILLINGADDRESS,
    SHIPPINGADDRESS,
    CURRENCY,
    CONSOLIDATED_EXCHANGE_RATE_UNIQUE_ID,
    SUBSIDIARY,
    NULL AS LOCATION,
    NULL AS LOCATIONNAME,
    CREATEDFROM,
    STATUS,
    DEPARTMENT,
    ACCOUNTINGBOOK,
    POSTING_PERIOD_DATE,
    ACCTNUMBER,
    ACCTTYPE,
    ACCOUNT_NAME,
    TYPE,
    TRANSACTIONNUMBER,
    NETAMOUNT,
    AMOUNT,
    CONVERTED_NET_AMOUNT,
    TRANSACTIONLINETYPE,
    ITEMTYPE,
    ACCOUNTINGLINETYPE,
    TITLE,
    QUANTITY,
    BOM_QUANTITY,
    MEMO,
    BILLINGSTATUS,
    NULL AS TRANSACTION_SESSION_RO,
    POSTING,
    TRANDATE,
    STARTDATE,
    ENDDATE,
    DUEDATE,
    CLOSEDATE,
    LASTMODIFIEDDATE,
    'NETSUITE' AS SOURCESYSTEM,
    'PLAYFLY' AS COMPANY,
    CURRENT_TIMESTAMP()::TIMESTAMP_NTZ AS CONSOLIDATED_GOLD_LOAD_DATE
from {{ env_var('DBT_PLAYFLY','playfly_dev') }}.gold.NETSUITE_FACT_TRANSACTION

union all


select
    TRANSACTIONS_UNIQUE_ID,
    TRANSACTION_ID AS TRANSACTION,
    TRAN_ID AS TRANID,
    TRANSACTION_LINE_ID AS TRANSACTIONLINE,
    TO_VARCHAR(ACCOUNT_ID) AS ACCOUNT,
    ITEM_ID AS ITEM,
    CLASS_ID AS CLASS,
    CHART_OF_ACCOUNTS_UNIQUE_ID AS CHART_OF_ACCOUNT_UNIQUE_ID,
    REPORTING_PERIOD_ID AS POSTINGPERIOD,
    NULL AS EMPLOYEE,
    NULL AS ENTITY,
    NULL AS BILLINGADDRESS,
    NULL AS SHIPPINGADDRESS,
    CURRENCY_ID AS CURRENCY,
    NULL AS CONSOLIDATED_EXCHANGE_RATE_UNIQUE_ID,
    SUBSIDIARY_ID AS SUBSIDIARY,
    LOCATION_ID AS LOCATION,
    LOCATION_NAME AS LOCATIONNAME,
    NULL AS CREATEDFROM,
    NULL AS STATUS,
    DEPARTMENT_ID AS DEPARTMENT,
    NULL AS ACCOUNTINGBOOK,
    POSTING_PERIOD_DATE,
    NULL AS ACCTNUMBER,
    NULL AS ACCTTYPE,
    NULL AS ACCOUNT_NAME,
    TO_VARCHAR(TRANSACTION_TYPE) AS TYPE,
    TRANSACTION_NUMBER AS TRANSACTIONNUMBER,
    NET_AMOUNT AS NETAMOUNT,
    AMOUNT,
    NULL AS CONVERTED_NET_AMOUNT,
    NULL AS TRANSACTIONLINETYPE,
    NULL AS ITEMTYPE,
    NULL AS ACCOUNTINGLINETYPE,
    TITLE,
    NULL AS QUANTITY,
    NULL AS BOM_QUANTITY,
    MEMO,
    NULL AS BILLINGSTATUS,
    NULL AS TRANSACTION_SESSION_RO,
    NULL AS POSTING,
    TRAN_DATE AS TRANDATE,
    NULL AS STARTDATE,
    NULL AS ENDDATE,
    NULL AS DUEDATE,
    NULL AS CLOSEDATE,
    LAST_MODIFIED_DATE AS LASTMODIFIEDDATE,
    'SAGE' AS SOURCESYSTEM,
    'SPOTLESS' AS COMPANY,
    CURRENT_TIMESTAMP()::TIMESTAMP_NTZ AS CONSOLIDATED_GOLD_LOAD_DATE
from {{ env_var('DBT_SPOTLESS','spotless_dev') }}.gold.SAGE_FACT_TRANSACTION
