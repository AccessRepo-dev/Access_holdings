version: 2

sources:
  - name: wagway_gold
    database: "{{ env_var('DBT_CONSOLIDATED', 'consolidated_dev') }}"
    schema: gold
    tables:
      - name: NETSUITE_DIM_ITEM
      - name: NETSUITE_DIM_EMPLOYEE
      - name: NETSUITE_DIM_ACCOUNTING_PERIOD
      - name: NETSUITE_DIM_ENTITY
      - name: NETSUITE_DIM_CURRENCYRATE
      - name: NETSUITE_DIM_CURRENCY
      - name: NETSUITE_DIM_SUBSIDIARY
      - name: NETSUITE_DIM_DEPARTMENT
      - name: NETSUITE_DIM_LOCATION
      - name: NETSUITE_DIM_CHART_OF_ACCOUNT
      - name: NETSUITE_DIM_BUDGET_CATEGORY
      - name: NETSUITE_FACT_TRANSACTION
      - name : NETSUITE_FACT_BUDGET

  - name: playfly_gold
    database: "{{ env_var('DBT_CONSOLIDATED', 'consolidated_dev') }}"
    schema: gold
    tables:
      - name: NETSUITE_DIM_ITEM
      - name: NETSUITE_DIM_EMPLOYEE
      - name: NETSUITE_DIM_ACCOUNTING_PERIOD
      - name: NETSUITE_DIM_ENTITY
      - name: NETSUITE_DIM_CURRENCYRATE
      - name: NETSUITE_DIM_CURRENCY
      - name: NETSUITE_DIM_SUBSIDIARY
      - name: NETSUITE_DIM_DEPARTMENT
      - name: NETSUITE_DIM_LOCATION
      - name: NETSUITE_DIM_CHART_OF_ACCOUNT
      - name: NETSUITE_DIM_BUDGET_CATEGORY
      - name: NETSUITE_FACT_TRANSACTION
      - name : NETSUITE_FACT_BUDGET